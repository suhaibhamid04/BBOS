rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Booking Bridge OS security rules
    // By default, allow read/write for authenticated users or public demo operations with valid payload structure
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
